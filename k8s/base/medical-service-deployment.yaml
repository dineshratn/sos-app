apiVersion: apps/v1
kind: Deployment
metadata:
  name: medical-service
  namespace: sos-app
  labels:
    app: medical-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: medical-service
  template:
    metadata:
      labels:
        app: medical-service
    spec:
      initContainers:
      - name: wait-for-postgres
        image: busybox:1.35
        command: ['sh', '-c', 'until nc -z postgres 5432; do echo waiting for postgres; sleep 2; done;']
      - name: wait-for-auth-service
        image: busybox:1.35
        command: ['sh', '-c', 'until nc -z auth-service 3001; do echo waiting for auth-service; sleep 2; done;']
      containers:
      - name: medical-service
        image: sos-app/medical-service:latest
        imagePullPolicy: Never  # Use local images built in Minikube
        ports:
        - containerPort: 3003
          name: http
        env:
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: sos-app-config
              key: NODE_ENV
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: sos-app-config
              key: MEDICAL_SERVICE_PORT
        - name: DATABASE_URL
          value: "postgresql://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@$(POSTGRES_HOST):$(POSTGRES_PORT)/$(MEDICAL_DB_NAME)"
        - name: POSTGRES_HOST
          valueFrom:
            configMapKeyRef:
              name: sos-app-config
              key: POSTGRES_HOST
        - name: POSTGRES_PORT
          valueFrom:
            configMapKeyRef:
              name: sos-app-config
              key: POSTGRES_PORT
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: sos-app-secrets
              key: POSTGRES_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: sos-app-secrets
              key: POSTGRES_PASSWORD
        - name: MEDICAL_DB_NAME
          valueFrom:
            configMapKeyRef:
              name: sos-app-config
              key: MEDICAL_DB_NAME
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: sos-app-secrets
              key: JWT_SECRET
        - name: AUTH_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: sos-app-config
              key: AUTH_SERVICE_URL
        - name: ENCRYPTION_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: sos-app-secrets
              key: ENCRYPTION_SECRET_KEY
        - name: ENCRYPTION_KEY_ID
          valueFrom:
            secretKeyRef:
              name: sos-app-secrets
              key: ENCRYPTION_KEY_ID
        - name: FRONTEND_URL
          valueFrom:
            configMapKeyRef:
              name: sos-app-config
              key: FRONTEND_URL
        - name: CORS_ORIGINS
          valueFrom:
            configMapKeyRef:
              name: sos-app-config
              key: CORS_ORIGINS
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
        readinessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: medical-service
  namespace: sos-app
  labels:
    app: medical-service
spec:
  type: NodePort
  ports:
  - port: 3003
    targetPort: 3003
    nodePort: 30003
    protocol: TCP
    name: http
  selector:
    app: medical-service
