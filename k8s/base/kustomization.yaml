apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sos-app

resources:
  - namespace.yaml
  - configmap.yaml
  - secrets.yaml
  - postgres-pvc.yaml
  - redis-pvc.yaml
  - postgres-deployment.yaml
  - redis-deployment.yaml
  - auth-service-deployment.yaml
  - user-service-deployment.yaml
  - medical-service-deployment.yaml

# Common labels applied to all resources
commonLabels:
  app.kubernetes.io/name: sos-app
  app.kubernetes.io/version: v1.0.0
  app.kubernetes.io/managed-by: kustomize

# Images can be customized here if needed
# images:
#   - name: sos-app/auth-service
#     newTag: v1.0.0
#   - name: sos-app/user-service
#     newTag: v1.0.0
#   - name: sos-app/medical-service
#     newTag: v1.0.0
